# MOVED. GitHub Actions only discovers workflows at the repository root, and this
# project lives in a subfolder of the fmuia/sda_projects monorepo — so a workflow
# here was silently never executed. The real, working CI now lives at the repo root:
#
#     ../../.github/workflows/ci.yml   (i.e. sda_projects/.github/workflows/ci.yml)
#
# It runs both the package tests and the headless notebook suite with
# `working-directory: causal-marketing-pymc`, triggers on `master`, and is scoped to
# `causal-marketing-pymc/**`. This stub is kept only so anyone who looks here is
# redirected; it is inert (GitHub ignores workflows outside the repo root).
